@model webapi.Models.Sales

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Sales</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MeetingRoomID" />
            <div class="form-group">
                <label asp-for="NomSala" class="control-label"></label>
                <input asp-for="NomSala" class="form-control" />
                <span asp-validation-for="NomSala" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="OfficeID" class="control-label"></label>
                <select asp-for="OfficeID" class="form-control" asp-items="ViewBag.OfficeID"></select>
                <span asp-validation-for="OfficeID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
