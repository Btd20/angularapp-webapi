@model IEnumerable<webapi.Models.Oficines>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomOficina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ciutat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomOficina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ciutat.CityID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OfficeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OfficeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OfficeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
